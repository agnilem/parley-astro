---
import { group, type Testimonial } from '../../lib/content';
const testimonials = await group<Testimonial>('testimonials');
---
<section class="testimonials bleed" id="testimonials">
  <header class="testimonials__header">
    <p class="badge">What people say</p>
    <h2 class="h2">Teams that work<br />with Parley, not around it</h2>
    <p class="testimonials__lede">
      From solo founders to enterprise teams — here's what our users have to
      say after making Parley their daily partner.
    </p>
  </header>

  <div class="marquee">
    <ul class="marquee__track" id="track" role="list">
      {testimonials.map((t) => (
        <li class="tcard">
          <p class="tcard__quote">{t.quote}</p>
          <figure class="tcard__author">
            <img class="tcard__avatar" src={t.avatar} srcset={t.srcset || undefined} alt="" />
            <figcaption>
              <h5 class="tcard__name">{t.name}</h5>
              <span class="tcard__role">{t.role}</span>
            </figcaption>
          </figure>
        </li>
      ))}
    </ul>
  </div>
</section>
