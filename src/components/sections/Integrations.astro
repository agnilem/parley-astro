---
import { group, type IntegrationRow } from '../../lib/content';
const rows = await group<IntegrationRow>('integrationRows');
---
<section class="integrations" id="integrations">
  <header class="integrations__header">
    <p class="badge">Integrations</p>
    <h2 class="h2 h2--center">
      Connect your workflow. <em>Parley meets you there.</em>
    </h2>
    <p class="integrations__lede">
      Slack, Linear, Notion, GitHub and 60+ more. Parley triggers actions,
      fetches context, and keeps things in sync — right where your team
      already works.
    </p>
  </header>

  <div class="int-marquee">
    {rows.map((row) => (
      <div class="int-marquee__row" data-dir={row.dir}>
        {/* Second set is the seamless-loop duplicate — decorative, so its
            tiles carry empty alt text. */}
        {[false, true].map((dup) => (
          <div class="int-marquee__set" aria-hidden={dup ? 'true' : undefined}>
            {row.tiles.map((t) => (
              <span class="int-tile"><img src={t.src} alt={dup ? '' : t.alt} /></span>
            ))}
          </div>
        ))}
      </div>
    ))}
  </div>
</section>
